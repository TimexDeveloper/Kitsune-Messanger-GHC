# 🚀 Vercel Deployment Guide

Это пошаговое руководство для развертывания Fox Messenger на Vercel.

## Предварительные требования

- ✅ GitHub аккаунт (https://github.com)
- ✅ Vercel аккаунт (https://vercel.com - бесплатно)
- ✅ Этот репозиторий скопирован локально

## 📋 Полная инструкция по развертыванию

### Шаг 1: Подготовка GitHub репозитория

#### 1.1 Инициализация Git (если еще не сделано)

```bash
cd fox-messenger
git init
git add .
git commit -m "Initial commit: Fox Messenger"
```

#### 1.2 Создание репозитория на GitHub

1. Перейдите на https://github.com/new
2. Введите имя репозитория: `fox-messenger`
3. Добавьте описание: "Modern web-based messenger"
4. Выберите Public или Private (по предпочтению)
5. ⚠️ НЕ инициализируйте с README (у нас уже есть)
6. Нажмите "Create repository"

#### 1.3 Загрузка кода на GitHub

```bash
git remote add origin https://github.com/ВАШ_ЮЗЕР/fox-messenger.git
git branch -M main
git push -u origin main
```

**Если получите ошибку аутентификации:**

- Используйте personal access token вместо пароля
- Перейдите на: https://github.com/settings/tokens
- Нажмите "Generate new token (classic)"
- Выберите scopes: `repo`, `workflow`
- Скопируйте токен и используйте как пароль

### Шаг 2: Подключение к Vercel

#### 2.1 Вход в Vercel

1. Перейдите на https://vercel.com/dashboard
2. Нажмите на профиль → "Settings"
3. Убедитесь, что вы подключены к GitHub

#### 2.2 Импорт проекта

1. Нажмите "Add New..." → "Project"
2. Нажмите "Import Git Repository"
3. Введите URL вашего репозитория: `https://github.com/ВАШ_ЮЗЕР/fox-messenger`
4. Нажмите "Continue"

#### 2.3 Конфигурация проекта

**Project Settings:**
- Framework Preset: **Next.js** (должно определиться автоматически)
- Root Directory: **.** (текущая директория)
- Build Command: `npm run build` (по умолчанию)
- Output Directory: `.next` (по умолчанию)
- Install Command: `npm install` (по умолчанию)

Нажмите "Continue"

### Шаг 3: Переменные окружения

#### 3.1 Обязательные переменные

В форме "Environment Variables", добавьте эти переменные:

**Вариант 1: Минимальная конфигурация (рекомендуется для начала)**

```
Имя: NEXTAUTH_SECRET
Значение: (сгенерируйте): openssl rand -base64 32

Имя: NEXTAUTH_URL
Значение: https://YOUR_APP_NAME.vercel.app
```

**Вариант 2: Полная конфигурация**

```
NEXTAUTH_SECRET = <сгенерированный ключ>
NEXTAUTH_URL = https://fox-messenger-demo.vercel.app
NEXT_PUBLIC_APP_NAME = Fox Messenger
NEXT_PUBLIC_APP_URL = https://fox-messenger-demo.vercel.app
NODE_ENV = production
```

#### 3.2 Как сгенерировать NEXTAUTH_SECRET

**Опция A: Используя терминал (Linux/Mac)**

```bash
openssl rand -base64 32
```

**Опция B: Используя PowerShell (Windows)**

```powershell
[System.Convert]::ToBase64String([System.Security.Cryptography.RandomNumberGenerator]::GetBytes(32))
```

**Опция C: Онлайн генератор**

https://generate-secret.vercel.app/

#### 3.3 Получение APP_URL

- Если вы уже знаете имя приложения Vercel: `https://your-app-name.vercel.app`
- Если нет: назовите его `fox-messenger-XXX` или используйте предлагаемое Vercel имя после развертывания

### Шаг 4: Развертывание

1. После ввода переменных окружения, нажмите "Deploy"
2. Vercel начнет сборку проекта (обычно 2-3 минуты)
3. Вы увидите "Congratulations!" когда развертывание завершится
4. Нажмите "Visit" для перехода на ваше приложение

### Шаг 5: Проверка развертывания

1. Откройте ваше приложение: `https://fox-messenger-XXX.vercel.app`
2. Введите имя гостя
3. Нажмите "Continue as Guest"
4. Проверьте работу интерфейса чата

## 🗄️ Опциональная настройка БД (Vercel Postgres)

### 5.1 Добавление PostgreSQL

1. В Vercel dashboard откройте ваш проект
2. Перейдите на вкладку **Storage**
3. Нажмите **Create**
4. Выберите **Postgres**
5. Выберите регион (ближайший к вам)
6. Нажмите **Create**

### 5.2 Автоматическая конфигурация

Vercel автоматически добавит эти переменные окружения:

```
POSTGRES_PRISMA_URL
POSTGRES_URL_NON_POOLING
POSTGRES_HOST
POSTGRES_PASSWORD
POSTGRES_USER
POSTGRES_DATABASE
```

Они появятся в Project Settings → Environment Variables

### 5.3 Инициализация таблиц

После подключения БД, запустите инициализацию:

```bash
# Локально с переменными Vercel
vercel env pull .env.local

# Или добавьте API endpoint для инициализации
curl https://your-app.vercel.app/api/db/init
```

## 💾 Опциональная настройка хранилища (Vercel Blob)

### 6.1 Добавление Blob Storage

1. В Vercel dashboard откройте ваш проект
2. Перейдите на вкладку **Storage**
3. Нажмите **Create**
4. Выберите **Blob**
5. Выберите регион
6. Нажмите **Create**

### 6.2 Автоматическая конфигурация

Vercel добавит:

```
BLOB_READ_WRITE_TOKEN
```

Эта переменная автоматически доступна в вашем приложении.

## 🔄 Обновление приложения после развертывания

### Сценарий 1: Обновления через Git

Все изменения в основной ветке автоматически развертываются:

```bash
# Внесите изменения локально
git add .
git commit -m "Your changes"
git push origin main

# Vercel автоматически начнет развертывание (обычно в течение минуты)
```

### Сценарий 2: Ручное развертывание

1. Откройте Vercel dashboard
2. Выберите ваш проект
3. Нажмите "Deployments"
4. Найдите последний коммит
5. Нажмите "Redeploy" если нужно

## 🐛 Решение проблем при развертывании

### Проблема: "Build failed"

**Решение:**

```bash
# Проверьте локально
npm run build

# Очистите кэш Vercel
vercel env pull
rm -rf .next
npm run build
```

### Проблема: "Missing environment variables"

**Решение:**

1. Перейдите в Vercel dashboard
2. Project → Settings → Environment Variables
3. Убедитесь что все переменные добавлены для Production
4. Перезагрузите развертывание (Redeploy)

### Проблема: "Connection refused" к БД

**Решение:**

1. Проверьте что `POSTGRES_PRISMA_URL` скопирован правильно
2. Убедитесь что БД активна в Storage tab
3. Перезагрузите окружение: `vercel env pull .env.local`

### Проблема: "Module not found"

**Решение:**

```bash
npm install
git add package-lock.json
git commit -m "Update dependencies"
git push origin main
```

## 📊 Мониторинг развертывания

### Просмотр логов

1. Vercel dashboard → Проект → Deployments
2. Выберите развертывание
3. Откройте вкладку "Logs"
4. Смотрите build и runtime логи

### Проверка здоровья приложения

1. Перейдите на `https://your-app.vercel.app/`
2. Откройте DevTools (F12)
3. Откройте вкладку Console
4. Проверьте на ошибки

## 🔐 Безопасность

### Рекомендации

1. ⚠️ **Никогда** не коммитьте `.env` или `.env.local`
2. ✅ Используйте `.env.example` для документации
3. ✅ Все sensitive переменные устанавливайте в Vercel dashboard
4. ✅ Регулярно ротируйте `NEXTAUTH_SECRET`
5. ✅ Используйте `https://` для `NEXTAUTH_URL`

### Обновление NEXTAUTH_SECRET

1. Вычислите новый ключ: `openssl rand -base64 32`
2. Перейдите в Vercel dashboard
3. Settings → Environment Variables
4. Отредактируйте `NEXTAUTH_SECRET`
5. Нажмите "Save" и затем "Redeploy"

## 📧 Контакты поддержки

- Vercel Support: https://vercel.com/support
- Next.js Docs: https://nextjs.org/docs
- GitHub Issues: https://github.com/yourusername/fox-messenger/issues

---

**Поздравляем! 🎉 Ваше приложение развернуто на Vercel!**
